# 🩸 بنك الدم - تطبيق الهاتف البسيط

تطبيق ويب بسيط جداً للهاتف لإدارة تبرعات الدم وشيكات الدم.

## 📱 المميزات

- **واجهة بسيطة جداً** مصممة خصيصاً للهاتف
- **3 وظائف أساسية فقط**:
  - ➕ إضافة متبرع جديد
  - 🩸 تسجيل تبرع
  - 🎫 إصدار شيك دم
- **إحصائيات سريعة** (عدد المتبرعين، التبرعات، الشيكات)
- **عرض آخر السجلات** بسرعة
- **تسجيل دخول مسؤول واحد فقط**

## 🚀 كيفية الاستخدام

### 1. إعداد Firebase

1. اذهب إلى [Firebase Console](https://console.firebase.google.com/)
2. أنشئ مشروع جديد
3. فعّل **Firestore Database**
4. اضبط قواعد الأمان:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2025, 1, 1);
    }
  }
}
```

### 2. تحديث إعدادات Firebase

افتح ملف `firebase-config.js` واستبدل القيم:

```javascript
const firebaseConfig = {
    apiKey: "مفتاح API الخاص بك",
    authDomain: "مشروعك.firebaseapp.com",
    projectId: "اسم مشروعك",
    // ... باقي الإعدادات
};
```

### 3. بيانات المسؤول

- **اسم المستخدم**: `admin`
- **كلمة المرور**: `blood123`

(يمكنك تغييرها في ملف `firebase-config.js`)

## 📁 هيكل الملفات

```
blood-app/
├── index.html              # الصفحة الرئيسية
├── style.css              # التنسيق البسيط للهاتف
├── script.js              # الوظائف الأساسية
├── firebase-config.js     # إعدادات Firebase
└── README.md              # هذا الملف
```

## 🎨 التصميم

- **ألوان بسيطة**: أحمر، أبيض، رمادي
- **أزرار كبيرة**: سهولة اللمس على الهاتف
- **خطوط كبيرة**: وضوح القراءة
- **تصميم متجاوب**: يعمل على جميع الهواتف

## 📱 كيفية النشر

### على Vercel (موصى به)

1. ارفع الملفات على GitHub
2. اربط حسابك بـ Vercel
3. اختر المستودع
4. انشر الموقع

### على Firebase Hosting

```bash
npm install -g firebase-tools
firebase login
firebase init hosting
firebase deploy
```

## 🔧 قاعدة البيانات

### المتبرعين (donors)
```javascript
{
  name: "اسم المتبرع",
  phone: "رقم الهاتف",
  bloodType: "فصيلة الدم",
  createdAt: timestamp,
  lastDonation: timestamp
}
```

### التبرعات (donations)
```javascript
{
  donorId: "معرف المتبرع",
  donorName: "اسم المتبرع",
  bloodType: "فصيلة الدم",
  amount: number,
  createdAt: timestamp
}
```

### شيكات الدم (vouchers)
```javascript
{
  voucherNumber: "رقم الشيك",
  donorId: "معرف المتبرع",
  donorName: "اسم المتبرع",
  bloodType: "فصيلة الدم",
  amount: number,
  issueDate: timestamp,
  expiryDate: timestamp,
  status: "issued"
}
```

## 📋 خطوات الاستخدام

1. **فتح التطبيق** على الهاتف
2. **تسجيل الدخول** ببيانات المسؤول
3. **إضافة متبرعين** جدد
4. **تسجيل التبرعات** اليومية
5. **إصدار شيكات الدم** عند الحاجة

## 🛠️ التعديلات

### تغيير بيانات المسؤول
في ملف `firebase-config.js`:

```javascript
const ADMIN_CREDENTIALS = {
    username: "اسم المستخدم الجديد",
    password: "كلمة المرور الجديدة"
};
```

### تغيير الألوان
في ملف `style.css`:

```css
:root {
    --primary-color: #e74c3c;  /* اللون الأحمر */
    --background-color: #f8f9fa;  /* الخلفية */
    --text-color: #2c3e50;  /* لون النص */
}
```

## 📞 الدعم

إذا واجهت أي مشاكل:

1. تأكد من إعدادات Firebase صحيحة
2. تحقق من اتصال الإنترنت
3. افتح وحدة التحكم في المتصفح للتحقق من الأخطاء

## 🎉 مبروك!

الآن لديك تطبيق بنك دم بسيط وجاهز للاستخدام على الهاتف!

---

**ملاحظة**: هذا التطبيق مصمم ليكون بسيطاً جداً وسريع الاستخدام. يمكنك إضافة المزيد من المميزات حسب الحاجة.
