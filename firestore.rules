rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // السماح بالوصول الكامل للقراءة والكتابة
    // في بيئة التطوير فقط
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
